# NAE Platform — Environment Variables
# Copy to .env and fill in your values.
# NEVER commit .env to version control.

# Tradier (if using Tradier broker)
TRADIER_ACCOUNT_ID=

# Alpaca (if using Alpaca broker)
# API keys go in config.yaml, not here
